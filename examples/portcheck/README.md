# PortCheck

A CLI tool to read and validate PORTS environment variable.

## Installation & Usage

To install:
```sh
make install
```

To run:
```sh
make run
```
Ensure your `PORTS` environment variable is set correctly before running.
