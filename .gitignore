# llama.cpp is a build dependency, not part of this project.
# Clone and build it separately -- see README.md.
llama.cpp/

# RAG index artifacts (rebuild with `purecoder ingest`)
*.npy
docstore.json
mylib_docs.json

# scaffolder output
build/
portcheck-out/

# python
__pycache__/
*.py[cod]
*.egg-info/
dist/
.venv/
venv/
.pytest_cache/
.ruff_cache/

# tooling / session state
.remember/
.claude/
*.deb

# editors / os
.vscode/
.idea/
.DS_Store

# live e2e scratch output
dashboardTest/

# local agent notes, not part of the repo
CLAUDE.md
.superpowers/

# the UI's installed deps and build output
UI/app/node_modules/
UI/app/dist/

# transcripts scripts/demo.sh keeps for a refused run
demo-*.log
